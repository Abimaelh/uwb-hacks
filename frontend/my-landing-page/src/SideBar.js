//this is mainly meant to use tailwind stuff to configure visual aspects of classes (colors, shape, etc.)
//nevermind tailwind is too much in too little time, we're doing vanilla CSS

const SideBar = () => {
    return {
        //<div className = "">
    
    }
};

export default SideBar;